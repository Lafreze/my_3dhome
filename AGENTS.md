# 项目约束

当前授权（2026-09-07）：用户明确要求将此项目推送到 https://github.com/Lafreze/my_3dhome.git 并部署到 Railway。允许为完成该请求提交、推送完整小屋代码及素材，创建对应 Railway 服务与公开域名，并配置该仓库的自动部署。本地运行能力需要保留。不得向其他平台发布或开启外网隧道。

线上清理仅允许使用 GPT 的直接工具，不得使用浏览器操作。

此前的 Sites 站点仍保持空白：2026-09-07 已通过独立临时目录发布空白版本 2，部署状态为 succeeded。旧部署版本及远端 Git 历史没有删除，因为当时工具不提供删除接口。新的 GitHub / Railway 授权不允许恢复该 Sites 站点或重新向 Sites 上传小屋。
