# 项目约束

当前授权（2026-09-08）：用户在新增「床上休息、角色定位和互动」的请求中明确要求完成后 push 到 Git 并部署到 Railway。本次授权覆盖此前仅本地运行的限制：完成检查后可以推送到既有 `Lafreze/my_3dhome` 仓库，并更新既有 Railway `my-3dhome` 服务。保留本地运行能力，不建立额外站点或外网隧道。

线上清理仍仅允许使用直接工具，不得使用浏览器操作。后续未授权的新服务、远端历史删除或资源清理不在本次发布范围内。

补充授权（2026-09-14）：用户要求升级咖啡厅、书房等房间的椅子模型，允许使用网络素材，并明确要求完成后部署线上。此次材质与椅子升级可在检查通过后推送到上述既有仓库，并部署到上述既有 Railway 服务。
