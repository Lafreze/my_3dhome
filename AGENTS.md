# 项目约束

当前授权（2026-09-07）：用户在要求保存电视 / 电脑网址、独立编辑画作及 Blender 修复王冠兔后，明确要求「完成后 push 到 git 并且部署到 railway」。允许提交并推送本项目代码和模型至 https://github.com/Lafreze/my_3dhome.git，更新现有 Railway 的 my-3dhome 生产服务。保留本地运行能力。不得向其他平台发布或开启外网隧道。

线上清理仅允许使用 GPT 的直接工具，不得使用浏览器操作。

此前 Sites 的空白站点不在本次发布范围内；不得恢复该 Sites 站点或向 Sites 重新上传小屋。新的明确授权取代此前仅限本地的限制，仅适用于上述 GitHub 与 Railway 目的地。
