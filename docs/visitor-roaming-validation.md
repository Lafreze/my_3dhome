# 访客记录与漫游验收 · 2026-09-15

## 本次验证

- Node.js 24.18.0：类型检查通过；所有修改的 TS / TSX / MJS 文件及开发配置的定向 lint 通过。
- 12 项管理员、访问记录与自由行走测试通过；55 座位的占用、休息、离座、共享互动与 HTTP 边界检查通过。
- 生产构建成功，仍有原项目的大体积客户端代码块提示。
- 本地浏览器验收桌面和 390 × 844 竖屏：进入/退出漫游，摇杆移动，角色与镜头跟随，互动按钮打开小风扇并产生回应，关闭卡片恢复漫游，原导航切换回来。
- 本地管理登录后可见来访簿，显示实际浏览测试产生的页面访问、单个匿名访客、停留与物件互动；手机可在面板内滚动查看分页记录。
- 服务器测试验证未登录、伪造登录及过期登录拒绝统计读取；同源、输入大小、会话归属、乱序去重、分页、敏感来源信息过滤和重启持久化均通过。

## 已有检查中的问题

- 全仓 lint 包含既有 UI 组件和旧测试错误；本次修改文件的定向检查通过。
- `check-visitor-travel.test.mjs` 的 `bedroom-reading|study-work` 自动换座路径检查在原始 HEAD 同样失败，已通过隔离导出原始文件复现。自由漫游使用连续家具碰撞检查，不使用该自动换座路径。
- `check-life.mjs` 期待旧的 `crownedRabbit` 障碍物条目，而原始 HEAD 的障碍物目录同样不存在该条目。本次未调整旧展品目录或重建历史换座路径。

## 适用范围

- 自由行走位置在当前页面显示；共享座位、休息和社交仍使用既有多人同步。
- 访问统计自本次上线开始积累，不能补回此前未记录的历史。独立访客按匿名浏览器 Cookie 计算，脚本拦截会影响停留与事件补充记录，但成功的页面 HTTP 请求仍由服务器记录。
- 持久化沿用现有 Railway 单实例与 `/data` 卷，未创建 PostgreSQL 或额外服务。多实例部署时需先迁移到共享数据库。
